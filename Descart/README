# --- Functions for reading the offset files generated by Descart

build the DescartLoadFunctions using:

 > f2py -m numpy.f2py -c -m DescartLoadFuncs DescartLoadFuncs.f90

Then import GetOffsets and call GetOffsets function to return a dictionary containing the masked data + filtering, and the offsets associated with the data.
